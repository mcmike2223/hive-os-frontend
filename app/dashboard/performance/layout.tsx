import type { Metadata } from "next";
export const metadata: Metadata = { title: "Performance Management - Hive.OS", description: "Employee goals, reviews, feedback, development, and performance analytics." };
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }

